spring.application.name=verifyemail
server.port=${SERVER_PORT:8080}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql: true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

spring.jpa.hibernate.ddl-auto=update

spring.datasource.url=jdbc:mysql://localhost:3306/verifyemaildb
spring.datasource.username=root
spring.datasource.password=Root@123

#spring.datasource.url=${DB_URL}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}
#disposable.domains=${DISPOSABLE_DOMAINS}

# Default hardcoded list for local development
disposable.domains=mailinator.com,10minutemail.com,tempmail.com,guerrillamail.com

